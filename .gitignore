
# Web Push VAPID keys — private key is a secret, never commit
cloud/vapid-keys.local.txt

# Local backup of the owner personal list (kept out of the public repo)
my-watchlist-backup.json

# Local dev tooling (impeccable design skill) — not app code, not for Pages
.claude/
.look-backup/
