# Xcode / build artifacts — never commit these
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.DS_Store
# Swift Package Manager
.build/
Packages/
# the generated project (regenerate from project.yml if you use xcodegen)
# *.xcodeproj
